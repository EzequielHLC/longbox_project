[tool.black]
line-length = 100
target-version = ["py313"]
extend-exclude = "migrations/"

[tool.ruff]
line-length = 100
extend-exclude = ["*/migrations/*"]

[tool.ruff.lint]
select = ["E", "F", "I", "DJ"]

[tool.ruff.lint.isort]
known-first-party = ["apps", "config"]